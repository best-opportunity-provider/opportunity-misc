{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "places": {
      "description": "A list of places where the opportunity is realized",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "description": "A name of place where the opportunity is realized",
            "type": "string"
          },
          "city": {
            "description": "A name of city where the opportunity is realized",
            "type": "string"
          },
          "country": {
            "description": "A name of country where the opportunity is realized",
            "type": "string"
          }
        },
        "required": ["name", "country"]
      }
    }
  }
}
